__pycache__/
*.pyc
.env
data/*.json
data/*.lock
*.log

# The seed and tracker workbooks ARE committed (see README): this is an
# ephemeral session container, so the repo is the only durable place to
# keep the working copy between sessions.
